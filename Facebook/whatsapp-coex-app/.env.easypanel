# Variáveis de Ambiente para Easypanel

## ⚠️ COPIE ESTAS VARIÁVEIS NO PAINEL DO EASYPANEL

```env
# WhatsApp Configuration
WHATSAPP_APP_ID=1335317331469574
WHATSAPP_CONFIG_ID=2031952424274683
WHATSAPP_APP_SECRET=SEU_APP_SECRET_AQUI
WHATSAPP_API_VERSION=v24.0

# Server Configuration
NODE_ENV=production
PORT=3000

# URLs (ATUALIZE COM SEU DOMÍNIO EASYPANEL)
FRONTEND_URL=https://seu-app.easypanel.host
REDIRECT_URI=https://seu-app.easypanel.host/callback
WEBHOOK_URL=https://seu-app.easypanel.host/webhook

# Webhook
WEBHOOK_VERIFY_TOKEN=CRIE_UMA_STRING_ALEATORIA_AQUI

# Sistema User Token (gerar no Facebook Business Manager)
SYSTEM_USER_TOKEN=SEU_SYSTEM_USER_TOKEN_AQUI

# Business ID
BUSINESS_ID=1132877482331513

# Encryption Key (32 caracteres - gere uma chave aleatória)
ENCRYPTION_KEY=abcdefghijklmnopqrstuvwxyz123456

# Nginx Port
NGINX_PORT=80
```

## 📝 O que você precisa substituir:

1. **WHATSAPP_APP_SECRET**
   - Pegar em: https://developers.facebook.com/apps/1335317331469574/settings/basic/

2. **FRONTEND_URL, REDIRECT_URI, WEBHOOK_URL**
   - Substituir `seu-app.easypanel.host` pelo domínio real do Easypanel
   - Exemplo: `https://whatsapp-coex-abc123.easypanel.host`
   - Ou use seu domínio custom: `https://whatsapp.seusite.com`

3. **WEBHOOK_VERIFY_TOKEN**
   - Criar string aleatória (ex: `meu_webhook_token_12345`)
   - PowerShell: `-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})`

4. **SYSTEM_USER_TOKEN**
   - Gerar em: https://business.facebook.com/settings/system-users
   - Permissões: `whatsapp_business_management` + `whatsapp_business_messaging`

5. **ENCRYPTION_KEY**
   - 32 caracteres aleatórios
   - PowerShell: `-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})`
   - Ou use: `abcdefghijklmnopqrstuvwxyz123456`

## ✅ Após configurar no Easypanel:

1. Atualize as URLs no Facebook:
   - OAuth Redirect URIs: `https://seu-dominio/callback`
   - Webhook URL: `https://seu-dominio/webhook`
   - App Domains: `seu-dominio`

2. Clique em **Deploy** no Easypanel

3. Aguarde build (3-5 minutos)

4. Acesse: `https://seu-dominio` e teste!
